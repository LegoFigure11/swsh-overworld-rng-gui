# .NET Desktop
# Build and run tests for .NET Desktop or Windows classic desktop solutions.
# Add steps that publish symbols, save build artifacts, and more:
# https://docs.microsoft.com/azure/devops/pipelines/apps/windows/dot-net

trigger:
- NotKoiTest

pr: none

pool:
  name: Azure Pipelines
  vmImage: 'windows-latest'
  demands:
  - msbuild
  - visualstudio

variables:
  Solution: '**/*.sln'
  Platform: 'x64'
  Configuration: 'Release'

steps:
- task: NuGetToolInstaller@1

- task: NuGetCommand@2
  inputs:
    command: 'restore'
    restoreSolution: '**/*.sln'
    
- task: DotNetCoreCLI@2
  inputs:
    command: 'restore'

- task: VSBuild@1
  inputs:
    solution: '$(Solution)'
    platform: '$(Platform)'
    configuration: '$(Configuration)'

- task: PublishBuildArtifacts@1
  displayName: 'Publish Artifact: SWSH_OWRNG_Generator_GUI'
  inputs:
    PathtoPublish: 'SWSH_OWRNG_Generator.WinForms\bin\Release\net6.0-windows'
    ArtifactName: SWSH_OWRNG_Generator_GUI
  condition: succeededOrFailed()
